// *******************************************************************
// File Name: Hello.java
//
// Purpose: Prints a Hello, World message
// *******************************************************************
 
public class Hello
{
   // ------------------------------------
   // main method -- prints the greeting
   // ------------------------------------
    public static void main (String args[]) {
       System.out.println ("Hello, World!");
   }
   // Answers:
   // 1. Deleting one l doesn't cause an error because it automatically switched the name
   //       of the class
   // 2. There is no error because what you say in the print message doesn't affect the 
   //       way the program is run. The difference is that it prints it with one l when run
   // 3. Without an ending quotation mark, the error is that it's an unclosed string literal
   // 4. Without a beginning quotation mark, the errors given are that it reached the end
   //       while parsing and it expects another paratheses and a semicolon.
   // 5. Without a semicolon after the statement, the program knows and the only error 
   //       is that it expects a semicolon.
 }